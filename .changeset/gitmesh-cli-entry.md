---
"gitmesh-agents": minor
---

Add the new `gitmesh` CLI entry with scaffold subcommands (doctor, init, migrate, apply, check, policy) and the full legacy command surface under `gitmesh legacy`; `gitmesh-agents` keeps its existing behavior unchanged (pivot T0.3).

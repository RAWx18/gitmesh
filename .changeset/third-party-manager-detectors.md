---
"@gitmesh/workspace-adapters": minor
---

Add the `third-party-managers` detector (pivot T1.7): a pure, read-only inventory of repos already managed by another config-sync tool, reported informationally as "managed by X" per ADR-004 - never a finding, never a destructive suggestion. Recognizes Ruler (`.ruler/ruler.toml` + recursive markdown sources + deprecated `.ruler/mcp.json`), rulesync (root `rulesync.jsonc`/`rulesync.local.jsonc`, the `.rulesync/` source tree, feature `.json(c)` files, deprecated ignore files), amtiYo agents-json (`.agents/agents.json` + local split), the agentsync family (`.agents/agentsync.toml`, `.agentsync/agentsync.toml`, `agentsync.config.json`, `agentsync.json`, `.agentsync-state.json`), agent_sync (`.ai/agent_sync.yaml` + `.ai/.sync-manifest`), GowayLee agent-sync (`.agent-sync.toml`), agentlink (`.agentlink.yaml`), the Vercel skills project lock (`skills-lock.json`) and mcp-lock (`.mcp.lock`), plus a symlink-topology scan that maps known agent config paths that are symlinks (a healthy zero-drift pattern) with their literal targets - a link resolving to a directory counts as healthy. Ships four golden fixtures including a Ruler-managed repo, a symlink-managed repo (materialized at test time via a `symlinks.json` sidecar, since git fixture trees cannot carry symlinks portably), and a negative case; exports the ADR-004 `COEXISTENCE_NOTE` for the doctor renderers.
